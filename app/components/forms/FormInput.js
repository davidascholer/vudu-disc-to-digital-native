import React, { useContext, useEffect, useState } from 'react';
import { Button, StyleSheet, Text, View } from 'react-native';

export default function FormInpu() {

    return(
        <Text>Form Input</Text>
    );
}

const styles = StyleSheet.create({

});